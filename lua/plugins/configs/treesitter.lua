local options = {
  ensure_installed = { "lua", "go", "python", "gomod", "yaml", "json" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
